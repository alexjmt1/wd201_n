<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Sign In</title>
</head>

<body>
    <%- include('header.ejs') %>
        <div class="w-full max-w-lg mx-auto mt-8 px-4 bg-white rounded-lg shadow-md">
            <h1 class="text-3xl font-semibold mt-4 mb-4">Change Password</h1>
            <form method="post" action="/changepassword" class="space-y-4">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                <label for="currentPassword" class="block">Password:</label>
                <input type="password" name="currentPassword" id="currentPassword" required
                    class="w-full border border-gray-300 rounded-md py-2 px-3">
                
                <label for="newPassword" class="block">New Password:</label>
                <input type="password" name="newPassword" id="newPassword" required
                    class="w-full border border-gray-300 rounded-md py-2 px-3">

                <label for="confirmNewPassword" class="block">Confirm Password:</label>
                <input type="password" name="confirmNewPassword" id="confirmNewPassword" required
                    class="w-full border border-gray-300 rounded-md py-2 px-3">

                <button type="submit"
                    class="bg-green-500 hover:bg-green-600 text-white py-2 px-4 mb-4 rounded-md w-full">Submit
                </button>
            </form>
        </div>
        <%- include('footer.ejs') %>
</body>

</html>